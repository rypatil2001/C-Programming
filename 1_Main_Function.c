// First C program

#include<stdio.h> // include standard input output (printf, scanf) header files.

int main()  //entry point function, the compiler searches it first
{
    printf("Hello World !!");
    printf("This is a Tribute to THE Dennis Ritchie...");

    return 0; // Indicates successfull Execution to the compiler (0 - because return type of main function is of inter format)
}